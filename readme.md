# Workspace-Management

## Prerequisites
- npm needs to be installed beforehand

## Installation
- clone the repository and install dependencies via ```npm install``` in the directory

## Usage
- to run the server: ```npm start```
- to run the tests: ```npm test```
